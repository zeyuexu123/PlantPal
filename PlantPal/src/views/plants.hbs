<h2 class="text-2xl font-bold mb-6 text-green-700 text-center">Your Plants</h2>

<div class="max-w-md mx-auto bg-white shadow-md rounded-xl p-6 space-y-4">
    {{#if plants.length}}
        <ul class="space-y-4">
        {{#each plants}}
            <li class="border border-gray-200 rounded-lg p-4 hover:bg-green-50 transition">
            <p class="font-semibold text-lg text-gray-800">{{name}}</p>
            <p class="text-gray-600">Species: {{species}}</p>
            <p class="text-gray-600">💧 Water every {{waterIntervalDays}} days</p>
            <p class="text-gray-600">
                Last watered: {{#if lastWatered}}{{lastWatered}}{{else}}N/A{{/if}}
            </p>
            </li>
        {{/each}}
        </ul>
    {{else}}
        <p class="text-gray-700 text-center">
        You don’t have any plants yet. 
        <a href="/plants/add" class="text-green-600 hover:underline">Add one here</a>!
        </p>
    {{/if}}

    <hr class="my-4 border-gray-300">

    <div class="text-center">
        <a href="/plants/add" class="inline-block bg-green-600 text-white font-semibold py-2 px-4 rounded-lg hover:bg-green-700 transition">
        ➕ Add a New Plant
        </a>
    </div>

    {{#if results}}
        {{!-- todo --}}
    {{/if}}
</div>